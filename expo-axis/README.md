Expo AXIS app

Run:

1. cd expo-axis
2. npm install
3. expo start

Notes:
- Enter robot IP on connection screen. The app connects to ws://[IP]:8765/ws and will auto-reconnect on drop.
- Controls are disabled when disconnected.
- Joysticks send continuous movement commands while held.
